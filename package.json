{
  "name": "falsh",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p ./build && cp *.* ./build"
  },
  "keywords": [],
  "author": "Voja YU4HAK",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.10.0"
  }
}
